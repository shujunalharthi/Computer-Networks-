# Question 3: IP Address Configuration

## LAN 3 IP Configuration
In this part, I configured the IP address for the devices in LAN 3. I entered the required IP address, subnet mask, and default gateway so the devices can communicate correctly through the network.

## LAN 4 IP Configuration
For LAN 4, I configured the network settings for the devices using the assigned IP addresses. I also added the correct subnet mask and default gateway to make sure the devices are connected to the correct network.

## Router 2 Interfaces
In this part, I configured the interfaces on Router 2 that connect LAN 3 and LAN 4. The correct IP addresses were assigned to the router interfaces so the router can provide communication between the different networks.
